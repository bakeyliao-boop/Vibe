# Vibe 协作与文档维护

## 沟通偏好

- 解释从当前任务的具体例子开始，说明发生了什么、为什么、下一步看什么，再给结论。
- 避免以助手第一视角主导叙述；使用直白语言，让用户能够自行判断。
- 根据用户基础控制术语密度；抽象概念先解释，再使用术语。
- 流程建议写明用户当前应做的动作、核对证据以及要避免的问题。

## 项目范围

- Vibe 当前是 `index.html` 中的生活内容共同收藏与回溯 Mock。预置样例的字段搜索与稳定查看全部已上线，并于 2026-09-12 获用户明确验收通过；本轮 Chrome 检查已通过。真实 iPhone 专项验收、目标小组找回与留存测试仍未完成。真实导入、账户与共同同步仍未完成；以最新代码和路线图为准。
- `legoland-shanghai-family-plan.html` 是同仓库独立页面，不应混入 Vibe 路线图或顺手修改。
- 保存用户已有改动；对有疑问的实现状态先查证，不从产品文案推断能力已存在。

## 每次核心讨论与交付的记录规则

用户已要求维护产品迭代路线，并为之后的每个核心讨论用 ADR 留档。

1. 在涉及 Vibe 产品工作时，先读 `ROADMAP.md` 和 `docs/adr/README.md`；再读当前议题对应 ADR。
2. 每个核心议题创建或更新一篇 ADR。范围、目标市场、核心交互、数据 / 导入 / 同步、访问边界、技术选型和发布 / 商业模式属于核心议题。
3. 同一议题后续讨论追加进展与未决问题；改变已接受决策时新建 ADR，并互链替代关系，保留旧理由。
4. 未确定的建议标“提案”。分别维护决策、实施、验证状态；不得把用户同意记录建议解释为同意实现全部建议。
5. 同步更新 Roadmap 的进度、迭代时间线及下一步。完成实现 / 发布才补相应状态与提交、检查 / 发布证据。
6. 历史回填注明建档日期、原讨论日期是否可确认；不得虚构时间、分享者、数据提取结果或市场验证。
7. 使用 `docs/adr/template.md`，按四位编号递增。只记录必要摘要，避免复制原始私人聊天、访问令牌或媒体数据。
8. 交付时提供主要文档入口和状态变化摘要。正常讨论期间维护文档，不另建定时或后台监听任务。

## 当前已确认的产品原则

- 分享不自动等于喜欢、想去或共同计划。
- 地点、类型、分享人查看的是共同内容池；来源应可追溯。
- 选中资源应有明确原文入口；未知可达性不应禁用打开。
- 自动浮现要松弛，主动选择要可控；当前节奏以 ADR-0007 为准。
- 查找与查看全部使用同页稳定网格，查找期间暂停自动变化；选中项、命中证据与原文入口保持一致。现有字段搜索不等于语义检索或真实导入；当前范围以 ADR-0008 为准。
- 用户已接受轻量保存、单人启动再邀请朋友、带真实线索再发现三个后续方向（ADR-0009、0010、0013）；方向已确定不代表具体接入 / 权限 / 排序细则或实现已完成。
- 施工前完整前端 Mock 的补齐建议见 ADR-0014；模拟流程与真实平台接入、权限、云同步分别记录状态。
- 首发市场仍按 ADR-0011 提案验证；不把视觉原型或本轮 Mock 验收作为产品市场匹配的证据。
