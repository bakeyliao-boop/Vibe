# Vibe 协作与文档维护

## 沟通偏好

- 解释从当前任务的具体例子开始，说明发生了什么、为什么、下一步看什么，再给结论。
- 避免以助手第一视角主导叙述；使用直白语言，让用户能够自行判断。
- 根据用户基础控制术语密度；抽象概念先解释，再使用术语。
- 流程建议写明用户当前应做的动作、核对证据以及要避免的问题。

## 项目范围

- Vibe 当前是 `index.html` 与 `lifecycle.js` / `lifecycle.css` 中的生活内容共同收藏与回溯 Mock。预置样例的字段搜索与稳定查看全部于 2026-09-12 获用户明确验收通过（最近用户验收基线 69edcf0）。最新产品代码 74ffe1b 已上线：在上一版 8163f7d 的空相册、改错 / 撤销、角色邀请与来源线索上，改为原平台发起的分享承接演示。Chrome 本机 14 组检查、公开页冒烟及线上三文件一致性核对通过，待本轮用户验收。真实 iPhone 专项、目标小组找回与留存测试仍未完成。平台真实解析、账户、服务端权限与共同同步仍未完成；以最新代码和路线图为准。
- 用户已明确纠正粘贴优先入口，见 ADR-0018：从小红书 / 抖音发起，选择 Vibe 后保留收到的信息；当前已上线（74ffe1b，本机 Mock），检查覆盖 4 组尺寸 / 主题设置，待用户验收。原粘贴版本及其检查保留为历史证据；真实 iPhone 系统分享仍未验证。
- `legoland-shanghai-family-plan.html` 是同仓库独立页面，不应混入 Vibe 路线图或顺手修改。
- 保存用户已有改动；对有疑问的实现状态先查证，不从产品文案推断能力已存在。

## 每次核心讨论与交付的记录规则

用户已要求维护产品迭代路线，并为之后的每个核心讨论用 ADR 留档。

1. 在涉及 Vibe 产品工作时，先读 `ROADMAP.md` 和 `docs/adr/README.md`；再读当前议题对应 ADR。
2. 每个核心议题创建或更新一篇 ADR。范围、目标市场、核心交互、数据 / 导入 / 同步、访问边界、技术选型和发布 / 商业模式属于核心议题。
3. 同一议题后续讨论追加进展与未决问题；改变已接受决策时新建 ADR，并互链替代关系，保留旧理由。
4. 未确定的建议标“提案”。分别维护决策、实施、验证状态；不得把用户同意记录建议解释为同意实现全部建议。
5. 同步更新 Roadmap 的进度、迭代时间线及下一步。完成实现 / 发布才补相应状态与提交、检查 / 发布证据。
6. 历史回填注明建档日期、原讨论日期是否可确认；不得虚构时间、分享者、数据提取结果或市场验证。
7. 使用 `docs/adr/template.md`，按四位编号递增。只记录必要摘要，避免复制原始私人聊天、访问令牌或媒体数据。
8. 交付时提供主要文档入口和状态变化摘要。正常讨论期间维护文档，不另建定时或后台监听任务。

## 当前已确认的产品原则

- 分享不自动等于喜欢、想去或共同计划。
- 地点、类型、分享人查看的是共同内容池；来源应可追溯。
- 选中资源应有明确原文入口；未知可达性不应禁用打开。
- 自动浮现要松弛，主动选择要可控；当前节奏以 ADR-0007 为准。
- 查找与查看全部使用同页稳定网格，查找期间暂停自动变化；选中项、命中证据与原文入口保持一致。现有字段搜索不等于语义检索或真实导入；当前范围以 ADR-0008 为准。
- 用户已接受轻量保存、单人启动再邀请朋友、带真实线索再发现三个方向（ADR-0009、0010、0013），并授权保留现有基调补齐完整前端 Mock；真实接入 / 权限 / 长期排序仍需另行决策。
- 施工前完整前端 Mock 的本轮范围与检查见 ADR-0014。双本机数据池、先成功写入再更新页面、输入来源及保守去重见 ADR-0015；不得自动覆盖无法读取的旧记录。
- 主保存路径以 ADR-0018 为准：Vibe 内仅提供“如何收下”说明，来源分享演示从手机框外“体验场景”进入；选择 Vibe 后不要求粘贴、填标题或再次必经确认。成功 / 重复以继续浏览来源为主，去 Vibe 查看可选；默认当前演示相册，切换须明确，不得为绕过重复暗中换库。
- 原平台页面 / 分享列表只是标明的概念示意，不断言小红书 / 抖音的真实按钮位置或一定能调起扩展。小红书演示用公开预置资料，抖音用明确合成资料；合成记录保留 `isFlowDemo` / `demoSourceId`，所有入口称“查看分享演示”并指向本站演示，不能冒充真实原文或移除标识。
- 邀请和角色规则见 ADR-0016：发起人维护全部、预览者只读、演示成员仅维护自己新增的内容；权限依据 `createdBy`，不能把原分享人当当前成员身份。这只是同浏览器流程演示，不是生产访问控制。
- `localStorage` 的示例 / 自己的相册用来隔离演示数据，不代表生产空间模型；“体验场景”位于产品手机框外，模拟状态与真实平台接入、权限、云同步分别记录状态。
- 首发市场仍按 ADR-0011 提案验证；不把视觉原型或本轮 Mock 验收作为产品市场匹配的证据。
- 首版真实解析范围已在用户要求转交的侧对话中收敛为小红书、抖音，见 ADR-0017；技术路线和供应商尚未选定。分别标记厂商文档、侧对话测试和本轮测试，不把短链展开或 HTTP 成功当成取得作品资料。
- 分享扩展入口及宿主传入字段按 ADR-0018 做真机验证，资料解析按 ADR-0017 单独验证；前端示意、Apple 机制说明均不能证明原平台实测可用，收到分享也不等于取得了原封面 / 完整正文。
- 原始解析样本位于被忽略的 `.local/`，仅用于本机后续评估，不随公共仓库发布；取得转交报告不等于获得向第三方解析服务提交样本或购买服务的授权。
- LLM 若参与接入，应基于已取得的资料提取关系；不能补造原封面 / 未取得正文，也不能将比较对象地名直接当目的地。现阶段这仍是待验证分工，不是已实现能力。
